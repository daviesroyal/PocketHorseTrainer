<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:viewModels="clr-namespace:PocketHorseTrainer.ViewModels"
             x:Class="PocketHorseTrainer.Views.AddHorsePage">
    
    <ContentPage.BindingContext>
        <viewModels:AddHorseViewModel/>
    </ContentPage.BindingContext>

    <ContentPage.Content>

        <StackLayout VerticalOptions="Center">

            <Entry Text="{Binding Name}"/>
            <Entry Text="{Binding Age}"/>
            <Picker ItemsSource="{Binding Barn}"/>
            <Picker ItemsSource="{Binding Breed}"/>
            <Picker ItemsSource="{Binding Color}"/>
            <Label Text="Markings"/>
            <Picker ItemsSource="{Binding Markings.FaceMarking}"/>
            <Picker ItemsSource="{Binding Markings.FrontLeft}"/>
            <Picker ItemsSource="{Binding Markings.FrontRight}"/>
            <Picker ItemsSource="{Binding Markings.BackLeft}"/>
            <Picker ItemsSource="{Binding Markings.BackRight}"/>

            <Button Command="{Binding AddCommand}"
                Text="Add Horse"/>

        </StackLayout>

    </ContentPage.Content>
    
</ContentPage>