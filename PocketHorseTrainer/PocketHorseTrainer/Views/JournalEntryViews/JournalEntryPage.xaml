<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:viewModels="clr-namespace:PocketHorseTrainer.ViewModels"
             x:Class="PocketHorseTrainer.Views.JournalEntryPage">

    <ContentPage.BindingContext>
        <viewModels:JournalEntryViewModel/>
    </ContentPage.BindingContext>
    
    <ContentPage.Content>
        
        <StackLayout>

            <Label Text="{Binding Date}"/>
            <Label Text="{Binding TimeHandling}"/>
            <Label Text="{Binding TimeRiding}"/>
            <Label Text="{Binding Discipline}"/>
            <Label Text="{Binding Weather}"/>
            <Label Text="{Binding Location}"/>
            <Label Text="{Binding Issues}"/>
            <Label Text="{Binding Strengths}"/>

            <!--add video and comments later-->

            <Button Text="Edit Journal Entry"
                    Clicked="OnEditButtonClicked"/>
            <Button Text="Delete Journal Entry"
                    Command="{Binding DeleteCommand}"/>
        
        </StackLayout>
    
    </ContentPage.Content>
    
</ContentPage>