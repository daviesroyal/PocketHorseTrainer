<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="PocketHorseTrainer.Views.GoalListPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:viewModels="clr-namespace:PocketHorseTrainer.ViewModels">

    <ContentPage.Content>

        <Button Clicked="OnAddGoalClicked" Text="Add New Goal" />

    </ContentPage.Content>

    <ListView
        x:Name="listView"
        Margin="20"
        ItemSelected="OnGoalSelected">
        <ListView.ItemTemplate>
            <DataTemplate>
                <ViewCell>
                    <StackLayout
                        Padding="20,0,0,0"
                        HorizontalOptions="StartAndExpand"
                        Orientation="Horizontal">
                        <Label Text="{Binding Name}" VerticalTextAlignment="Center" />
                        <Label Text="{Binding TimeSpan}" />
                        <Label Text="{Binding AreaOfImprovement}" />
                        <!--  if completed, do not show or show greyed out  -->
                        <Label Text="{Binding Completed}" />
                    </StackLayout>
                </ViewCell>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>

</ContentPage>