<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="PocketHorseTrainer.Views.HorsePage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:viewModels="clr-namespace:PocketHorseTrainer.ViewModels"
    Shell.TabBarIsVisible="False">

    <ContentPage.BindingContext>
        <viewModels:HorseProfileViewModel />
    </ContentPage.BindingContext>

    <StackLayout Margin="20">

        <Label Text="{Binding Name}" />
        <Label Text="{Binding Age}" />
        <Label Text="{Binding Barn}" />
        <Label Text="{Binding Breed}" />
        <Label Text="{Binding Color}" />
        <Label Text="{Binding Markings}" />

        <Button Clicked="OnGoalButtonClicked" Text="Training Goals" />
        <Button Clicked="OnJournalButtonClicked" Text="Training Journal" />

        <Button Clicked="OnEditButtonClicked" Text="Edit Horse" />
        <Button Command="{Binding DeleteCommand}" Text="Remove Horse" />

    </StackLayout>

</ContentPage>